load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_library")

rust_binary(
    name = "hello",
    srcs = ["main.rs"],
    deps = [":mylib"],
    edition = "2021",   # 👈 必须写

)

rust_library(
    name = "mylib",
    srcs = ["lib.rs"],
    edition = "2021",   # 👈 必须写

)
